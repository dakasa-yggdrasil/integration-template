services:
  rabbitmq:
    image: rabbitmq:3.13-management
    ports:
      - "5672:5672"
      - "15672:15672"

  integration-template:
    depends_on:
      - rabbitmq
    ports:
      - "${HEALTHCHECK_PORT:-8080}:${HEALTHCHECK_PORT:-8080}"
