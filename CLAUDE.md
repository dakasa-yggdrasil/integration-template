# CLAUDE

Start with `AGENTS.md`.

Extra guidance for Claude-style assistants:
- Preserve the standalone nature of this integration.
- Avoid “helpful” abstractions that hide contract drift between `describe` and `execute`.
- When editing capabilities, verify tests and examples in the same pass.
